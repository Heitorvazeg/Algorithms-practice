#include <bits/stdc++.h>

using namespace std;

int main() {
    ios::sync_with_stdio(false);
    cin.tie(0); 

    int n;

    for (int i = 0; i < n; i++) {
        int a;
        cin >> a;

        vector<int> v(a, 0);
        for (int j = 0; j < (int)v.size(); j++) {
            cin >> v[j];
        }

        int c = 0;
        int it = 1;
        for (int j = 0; j < (int)v.size(); j++) {
            if (v[])
        } 
    }
}